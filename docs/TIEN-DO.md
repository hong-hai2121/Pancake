# TIẾN ĐỘ CRM — mở file này khi quên đang làm tới đâu

> Chi tiết từng bước (làm gì, vì sao, "xong khi" nào) xem
> [THU-TU-TRIEN-KHAI-CRM.md](THU-TU-TRIEN-KHAI-CRM.md).
> Tiến độ RIÊNG 80 màn hình (màn nào thật/khung/chưa, route, làm ở lát nào) xem
> [TIEN-DO-MAN-HINH.md](TIEN-DO-MAN-HINH.md).
> Ký hiệu: ✅ xong · 🔨 đang làm · ⬜ chưa làm.
> **Quy ước cập nhật:** xong bước nào thì đổi ký hiệu + thêm 1 dòng vào Nhật ký cuối file.

## Phần A — Nền tảng

| | Bước | Nội dung một dòng |
|---|---|---|
| ✅ | **A1** | Nạp schema `crm` vào Postgres — *58 bảng đã có trong DB (kiểm 31/07/2026)* |
| ✅ | **A2** | **Đăng nhập** — JWT, 6 API `/api/v1/auth/*`, màn `/dang-nhap`, khoá toàn bộ web, seed 9 vai trò + 11 quyền + admin — *xong 31/07, nghiệm thu 27/27 ([A2-DANG-NHAP.md](A2-DANG-NHAP.md))* |
| ✅ | **A3** | Khung API chuẩn — bao `{success,data,message}`, 12+5 mã lỗi (`core/errors.py`), phân trang trần 100, `require_permission` — *xong 31/07* |
| ✅ | **A4** | Audit log — mọi thao tác ghi lưu ai/làm gì/**chỉ trường đổi** cũ→mới; API AUDIT-001/002 + màn Nhật ký — *xong 31/07* |
| ✅ | **A6** | **Cài đặt hệ thống trên web** (màn 78 · SYSTEM-001/002) — 19 công tắc + nhịp chạy worker gom vào `/quan-tri/cai-dat`, đổi là có hiệu lực ~10 giây, KHÔNG restart; bảng `crm.app_settings` chỉ giữ phần đã đổi, còn lại lấy .env; token/mật khẩu cố ý KHÔNG lên web — *39/39 PASS (`scripts/thu_cai_dat.py`), xong 01/08* |
| ✅ | **A5** | Quản lý người dùng — USER-001…006 + ROLE + TEAM + reset mật khẩu; 3 màn Quản trị; khoá đòi chuyển khách trước (FR-002); quyền nay 14 (thêm `user.manage`, `audit.view`; 31/07 thêm `user.manage_team` — trưởng nhóm tự tạo tài khoản đội mình) — *xong 31/07, nghiệm thu 30/30 + 22/22* |

## Phần B — 11 lát cắt nghiệp vụ (làm theo thứ tự)

| | Lát | Nội dung một dòng |
|---|---|---|
| ✅ | **B1** | Khách hàng 360° — luật + 14 API xong, 38/38 PASS ([B1-KHACH-HANG.md](B1-KHACH-HANG.md)); *màn 8/9/10 chờ khung template B11* |
| ✅ | **B2** | Nối Pancake vào CRM — `crm_sync` 11/11 PASS; công tắc `CRM_SYNC_ENABLED` mặc định **TẮT**; backfill kho cũ: `scripts/backfill_crm_tu_watcher.py` |
| ✅ | **B3** | Lead & pipeline Sale — luật 25/25 + API 16/16 PASS ([B3-LEAD-PIPELINE.md](B3-LEAD-PIPELINE.md)); *riêng Kanban màn 11 chờ khung template B11* |
| ✅ | **B4** | Công việc — task engine chung TASK-001…009, luật mục 19 (owner+hạn bắt buộc, không đóng thiếu kết quả), worker báo quá hạn, màn 12/26 đổ dữ liệu thật — 41/41 PASS (`scripts/thu_b4.py`) |
| ✅ | **B5** | Hồ sơ tư vấn — FR-050…053 · 17 API (CONSULT-001…005 · SYMPTOM-001…003+1 · MEDICAL-001…004 · SAFETY-001…005): red flag → cờ đỏ + chặn đề xuất + việc chuyển chuyên môn — **37/37 PASS** (`scripts/thu_b5.py`); *màn 13-14 chờ khung B11; FR-051 mới nhận file_url — upload ảnh khám thật kéo theo FILE-001…004 (xem Việc lẻ)* |
| ✅ | **B6** | Sản phẩm & liệu trình — FR-060…062 · 22 API (PRODUCT-001…008 · TREATMENT-001…014): versioning giá, rule engine trong DB, đề xuất chặn cờ đỏ + duyệt chuyên môn — **43/43 PASS** (`scripts/thu_b6.py`); *màn 42-46 chờ khung B11* |
| ✅ | **B7** | Đơn hàng — FR-080…082 · 11 API ORDER-001…011: 11 trạng thái + luật chuyển (lịch sử không xoá), ánh xạ 17 mã POS→CRM trong DB `order_status_mappings` (admin sửa màn 23, đồng bộ ăn ngay), đơn đầu/mua lại tự phân loại (hủy không tính, so theo mốc tạo bên POS), giá chốt thời điểm bán, đồng bộ idempotent theo (pos_shop_id, pos_order_id) + worker poll bù `POS_SYNC_ENABLED` mặc định **TẮT** — **50/50 PASS** (`scripts/thu_b7.py`); *backfill 53k đơn cũ: `scripts/backfill_don_pos.py` (chưa chạy); order_items từ POS chưa đồng bộ (SP POS chưa ánh xạ crm.products — nguyên văn nằm ở orders.pos_raw); webhook POS làm khi có domain; màn 21-23 chờ khung B11* |
| ✅ | **B-TH** | **Tích hợp Pancake (BRD mục 4)** — kết nối nhiều page/tài khoản, đồng bộ khách·hội thoại·thẻ·nhân viên·đơn, lưu vết `source/updated_at_external/synced_at`, nhật ký + hàng đợi lỗi có retry, quy nguồn ad (first/last touch), nút mở hội thoại Pancake; 4 màn `/quan-tri/tich-hop/*` + 13 API — **58/58 PASS** (`scripts/thu_muc4.py`) *(làm chen trước B8 theo yêu cầu 01/08)* |
| ✅ | **B-QC** | **Nguồn quảng cáo (BRD mục 4 phần Ads)** — cây campaign→adset→ad→creative + **CHI PHÍ theo ngày** lấy từ **Pancake POS Ads Manager** (khỏi cần Facebook Ads API); ROAS = doanh thu đơn đã giao / chi phí cùng kỳ, LTV, phễu, lý do chưa chốt; màn 7 + 53-55 + 56 (`/crm/quang-cao`), 12 API ADS + ATTRIBUTION, quyền mới `ads.view` — **47/47 PASS** (`scripts/thu_quang_cao.py`) |
| ✅ | **B8** | Bàn giao Sale→CSKH — FR-090/091 · 8 API HANDOVER-001…006 (+list/+PUT) · **màn 24-25 THẬT** (`/crm/ban-giao`): đơn giao thành công tự sinh phiếu (hook trong `order_service.change_status`, idempotent theo đơn), gán CSKH vòng tròn theo tải, chép liệu trình + mồi hồ sơ B5, tạo vỏ `care_plans` + việc onboarding; 8 trường bắt buộc — thiếu KHÔNG nhận được, trả lại Sale kèm việc, bổ sung đủ tự quay lại chờ nhận; nhận xong khách chính thức thuộc CSKH (`customer_assignments`); backfill KHÔNG sinh phiếu (`ban_giao=False`) — **41/41 PASS** (`scripts/thu_b8.py`) |
| ✅ | **B9** | Chăm sóc 11 bước — FR-100…110 · 28 mã API (CARE · CARE-STEP · ASSESSMENT · NORESPONSE): mốc CS04-08 sinh ĐÚNG ngày 4/10/15/20/25 từ **ngày bắt đầu dùng thật** (FR-102, chưa bắt đầu = chưa sinh); phiếu 11 bước MỘT cửa, trường bắt buộc + 7 bộ giá trị đọc từ `ref_codes`; automation cứng AU02…AU11 (3 lần không gặp → báo Sale, phản ứng → ca chuyên môn + C05, ngày 20 → cơ hội mua lại, ngày 25 chưa mua → bắt lý do + CS09, khách đòi dừng → do_not_contact); chuỗi không phản hồi nhắn→gọi→nhắn→gọi, đủ 4 → C08; worker `care-steps`; **màn 27 + 28-38 đều THẬT** (`/crm/cham-soc` — kể cả chuỗi không phản hồi + nút ngừng liên hệ trên web) — **64/64 PASS** (`scripts/thu_b9.py`) |
| ✅ | **B10** | Mua lại & khách ngủ — FR-120…123 · REPURCHASE-001…010: FR-120 ngày hết = bắt đầu THẬT × hệ số tuân thủ (thiếu liều ×1.25) + tạm dừng + hàng cũ, lưu vào liệu trình + đồng bộ cơ hội; FR-122 **9 nhãn suy từ ngày** (không lưu cột, khỏi job đêm) + chuyển bước có luật, 'Chưa mua' bắt lý do chuẩn 9 mã BRD (`lead_reasons`); FR-123 khách ngủ rổ 30/60/90/180 + lọc giá trị + gán chiến dịch (`reactivation_*`) + việc `mua_lai` + **doanh thu tái kích hoạt đo TỰ ĐỘNG**; đơn giao TC → cơ hội tự 'won' + member 'converted' (hook order_service); **màn 39-40-41 THẬT** (`/crm/mua-lai` · `/crm/khach-ngu`) — **40/40 PASS** (`scripts/thu_b10.py`) |
| ✅ | **B11** | Báo cáo cơ bản — FR-170…173 · 12 route REPORT-001…011: **sổ METRICS** (24 chỉ số, mỗi chỉ số khai MỘT lần FROM+WHERE) bảo đảm số tổng và drill-down CÙNG điều kiện lọc (FR-173); quyền theo nội dung (doanh thu=revenue.view · ads=ads.view, drill kiểm TỪNG metric); export CSV BOM UTF-8 đòi `data.export` + audit (FR-181); lead vào bước đếm theo LỊCH SỬ FR-041; **màn 4-5-6 + 60-62 + 64 THẬT** (`/crm/tong-quan` biểu đồ SVG + phễu 6 bậc bấm được · `/crm/dashboard-sale` · `/crm/dashboard-cskh` · `/crm/bao-cao` 7 tab · trang drill-down) — **46/46 PASS** (`scripts/thu_b11.py`); *màn 63 + điểm AI chờ tổng đài C-MVP3.* **→ PHẦN B HOÀN TẤT** |

## Phần C — Giai đoạn sau (chưa đụng tới khi B chưa xong)

| | Giai đoạn | Nội dung một dòng |
|---|---|---|
| ⬜ | **C-MVP3** | Tổng đài — ghi âm, transcript, AI chấm cuộc gọi — *FR-130…133 · CALL-001…005 + 2 webhook · AI-CALL-001…006 · màn 18-20 + 74* |
| ⬜ | **C-MVP4** | Kho kiến thức — duyệt nội dung, cây kịch bản, nâng cấp RAG hiện có — *FR-140…143 · KNOWLEDGE-001…009 · SCENARIO-001…006 · AI-001…008 + CONV-007/008 · màn 47-52* |
| 🔨 | **C-MVP5** | Facebook Ads — *phần lõi ĐÃ LÀM ở B-QC qua Pancake POS Ads Manager (ADS-002…008·010 · ATTRIBUTION-001/002 · FUNNEL-004 · màn 7 + 53-56)*. Còn lại: màn 57 báo cáo băn khoăn · màn 58 báo cáo lý do chưa chốt toàn cảnh · màn 59 trung tâm đề xuất Marketing (AI) · màn 75 tích hợp Facebook Ads trực tiếp (chỉ cần khi muốn số ngoài phạm vi Pancake) · FUNNEL-001…003 · nhận định AI của ADS-007 · ADS-009 |

## Việc lẻ ngoài lộ trình (ghi để khỏi quên)

- ⬜ Xoá tab **Thử API** (`/data/thu-api`) trước khi đưa web ra Internet — đang phơi token Pancake (từ A2 phải đăng nhập mới xem được, nhưng vẫn nên xoá)
- ✅ Tắt `/docs` `/redoc`, chặn `/poller` — *làm luôn trong A2 (middleware khoá + docs_url=None)*
- ⬜ Đưa lên domain: Cloudflare Tunnel + Access (đã bàn 31/07, làm sau A2)
- ✅ **Dò API Pancake POS** *(01/08)* — Open API riêng `https://pos.pages.fm/api/v1`, xác thực query `api_key` (tạo ở POS: Cấu hình → Nâng cao → Tích hợp bên thứ 3 → tab API Key; đã lưu `.env`: `PANCAKE_POS_API_KEY` + `PANCAKE_POS_SHOP_ID=1328220779`). Spec 82 endpoint: `https://docs.pancake.biz/pos/api/openapi.json`. Dò thật bằng `scripts/do_pos_api.py`: shop "Bác sĩ Hội" 56 page — **53.306 đơn** (102 trường/đơn, CÓ `conversation_id`/`page_id`/`post_id`/`ad_id` → nối được về `crm.conversations`), 29.691 khách (`fb_id`, `phone_numbers`, `order_count`, `purchased_amount`), 34 mẫu mã SP. 17 mã `status` (0 Mới · 17 Chờ XN · 1 Đã XN · 2 Đã gửi · 3 Đã nhận · 16 Đã thu tiền · 4/15/5 hoàn · 6 Hủy · 7 Xóa…) → nguồn ánh xạ 11 trạng thái CRM. Webhook có sẵn: `PUT /shops/{id}` bật `webhook_types:["orders",…]` → B7 nên đi webhook + poller bù
- ⬜ Commit đợt tái cấu trúc thư mục (đang nằm ở working tree, chưa commit)

**Nhóm đặc tả CHƯA thuộc lát nào** (phát hiện khi đối chiếu 4 file `docs/` 31/07 — ghi để khỏi lọt):

- ⬜ **Upload file đính kèm** FILE-001…004 — cần sớm nhất ở **B5** (FR-051: ảnh/PDF kết quả khám) → làm luôn trong B5
- ✅ **API hội thoại phía CRM** CONV-001…006 + PANCAKE-010 — *XONG 01/08 (33/33 PASS `scripts/thu_conv.py`)*: 7 API `/api/v1/conversations*` (danh sách · chi tiết · tin nhắn · gắn khách · gán NV · gửi tin · external-link), luật ở `services/conversation_service.py`; đọc thuần từ `crm.*` không gọi Pancake (trừ CONV-006 gửi thật). Còn CONV-007/008 (AI) thuộc C-MVP4; MÀN hiển thị (tab Hội thoại màn 9) vẫn chờ B11
- ⬜ **Webhook Pancake/POS** — mục 4 đòi "có cơ chế webhook nếu hỗ trợ"; hiện đi POLL BÙ + hàng đợi retry (đủ dùng, không mất dữ liệu). Bật webhook khi app có domain công khai: POS `PUT /shops/{id}` với `webhook_types:["orders",…]`; nhận vào thì gọi thẳng `pos_sync.sync_batch(..., run_type="webhook")` — nhật ký/hàng đợi lỗi dùng lại nguyên
- ✅ **Đồng bộ `messages` (nội dung tin nhắn) về `crm.messages`** — *XONG 01/08 cùng CONV-001…006*: worker `msg-sync` riêng (công tắc `MSG_SYNC_ENABLED` mặc định **TẮT**, nhịp/mẻ chỉnh ở màn Cài đặt), chỉ kéo hội thoại có `external_updated_at` mới hơn `messages_synced_at`; lịch sử cũ chạy tay `scripts/backfill_tin_nhan.py`
- ✅ **Trung tâm thông báo** màn 3 + NOTIFY-001…004 — *XONG 01/08 (39/39 PASS `scripts/thu_thong_bao.py`)*: DB 74 bảng (thêm `notifications` + `notification_settings`), worker `notify-scan` QUÉT 11 nguồn 5'/lần (không nhét lệnh gửi vào service nghiệp vụ), dedupe theo `(user_id, dedupe_key)`, màn `/crm/thong-bao` + chuông đếm chưa đọc trên mọi trang. *B9/B10 chỉ cần đổ dữ liệu vào bảng nguồn là thông báo tự chạy — 2 loại `khach_den_han_mua_lai` (B10) và phần phản ứng từ phiếu chăm (B9) hiện chưa có dữ liệu*
- ⬜ **Quản lý danh mục dùng chung** màn 72 + MASTER-001…007 — dữ liệu đã seed vào `ref_codes` (78 mã), chưa có API/màn cho Admin tự sửa; chưa gấp, seed lại được
- 🔨 **Cấu hình công ty · sao lưu · lỗi hệ thống** màn 78-80 + SYSTEM-001…003 · BACKUP-001…003 (FR-182) — *màn 78 (SYSTEM-001/002: công tắc + nhịp worker) ĐÃ XONG 01/08 ở `/quan-tri/cai-dat`*. Còn: thông tin công ty (tên/logo/múi giờ), SYSTEM-003 nhật ký lỗi hệ thống (màn 80), BACKUP-001…003 (màn 79) — để sau phần C

---

## Nhật ký

| Ngày | Việc |
|---|---|
| 01/08/2026 | **TRẢ NỢ GIAO DIỆN — 18 màn còn thiếu → màn hình đạt 62 ✅ / 3 🔨 / 15 ⬜, riêng 18 màn MVP đạt 17 ✅ / 1 🔨.** Làm theo [TIEN-DO-MAN-HINH.md](TIEN-DO-MAN-HINH.md) chứ không theo lát backend (phần B đã xong). **Màn 13-14-15** gộp một khu `/crm/tu-van/{id}` 3 tab vì Sale làm liền mạch (nhìn hội thoại → khai thác → đề xuất), tách 3 URL thì mất ngữ cảnh khách: tab tư vấn bày song song hội thoại ↔ checklist 7 câu bắt buộc + sàng lọc + gợi ý liệu trình (checklist SUY TỪ dữ liệu đã nhập, không hỏi lại người dùng); tab khai thác nhập triệu chứng theo FR-050 + 11 mục sàng lọc, ghi mục ĐỎ là cờ đỏ + mở ca ngay; tab đề xuất chạy rule engine B6 và **phân biệt rõ hai tình huống** — cờ đỏ CHẶN (nói rõ phải qua chuyên môn gỡ) khác với thiếu dữ liệu (chỉ đường sang khai thác), không gộp thành "có lỗi". **Màn 43/45/46** chi tiết SP (nội dung Sale được nói / **nội dung CẤM** tách riêng + lịch sử phiên bản giá) · chi tiết mẫu liệu trình · cấu hình luật (3 loại: loại trừ/phù hợp/cảnh báo, thêm luật ngay trên web, engine đọc DB nên ăn ngay). **Màn 16-17** bám đuổi + chuỗi chạm. **Màn 57-58** tỷ trọng lý do chưa chốt theo Sale/quảng cáo. **Màn 68** nhóm & ca + quy tắc chia lead. **Màn 69+71** bảng THEO DÕI 14 automation đang chạy thật (KHI · THÌ · nằm ở đâu · công tắc nào) — cố ý KHÔNG bày bảng cấu hình giả vì builder Khi-Nếu-Thì (màn 70) chưa có, bày ra sẽ khiến người dùng tưởng sửa được. **Màn 72** danh mục dùng chung (78 mã `ref_codes`, thêm mã trên web, mã cũ **ngừng dùng** chứ không xoá). **Hoàn thiện 8 + 21**: dùng lại bộ lọc CUSTOMER-001/B7 có sẵn thay vì viết truy vấn mới; ô trạng thái ở màn 21 bấm được để lọc nhanh. File mới: `profile_repo` mở rộng · `catalog_screen_repo` · `views/consult.py` · `views/catalog.py` · `views/ops.py`. **Vá 3 lỗi Python 3.12-only** lọt vào (f-string lồng nháy + backslash trong biểu thức) — máy này 3.11. Kiểm: mở thật toàn bộ 24 route mới bằng tài khoản admin (0 lỗi) + `thu_man_hinh.py` 40/40; hồi quy 10 bộ chính **410/410 xanh**. *15 màn ⬜ còn lại KHÔNG phải thiếu giao diện mà thiếu backend (tổng đài · kho kiến thức · AI · automation builder · sao lưu) — đã ghi rõ "cần xây gì trước" ở cuối file màn hình* |
| 01/08/2026 | **B11 XONG — Báo cáo cơ bản (FR-170…173) → PHẦN B (A1…B11) HOÀN TẤT.** Trái tim là **sổ METRICS** (`report_repo.py`, 24 chỉ số): mỗi chỉ số khai đúng MỘT lần (FROM + WHERE + cột danh sách) — số tổng (count/sum) và drill-down REPORT-010 chạy CÙNG điều kiện nên không bao giờ lệch (FR-173 "danh sách chi tiết dùng cùng điều kiện lọc với số tổng"); lọc kỳ `tu/den` + lọc người `user_id` ăn cả hai phía; lead vào bước (tư vấn/báo giá/chốt) đếm theo LỊCH SỬ FR-041 chứ không phải trạng thái hiện tại. 12 route `/api/v1/reports/*` phủ REPORT-001…011 (REPORT-008 chất lượng cuộc gọi trả khung trung thực chờ C-MVP3, không số ảo); quyền theo NỘI DUNG: doanh thu/đơn `revenue.view` · marketing `ads.view` · drill-down kiểm quyền TỪNG metric · export thêm `data.export` + audit `report_export` (FR-181), CSV BOM UTF-8 chấm phẩy. Web: **màn 4 làm THẬT** (18 ô + 3 tỷ lệ, ô doanh thu/chi phí TỰ ẨN theo quyền, biểu đồ doanh thu theo ngày SVG thuần, phễu 6 bậc — MỌI Ô bấm ra `/crm/bao-cao/chi-tiet`); **màn 5-6** dashboard từng Sale/CSKH (quản lý đổi người, số cá nhân + tỷ lệ phễu); **màn 60-62+64** ở `/crm/bao-cao` 7 tab (Sale mỗi người 1 dòng + 4 tỷ lệ; CSKH mốc đúng/quá hạn + DT mua lại; Marketing chi phí/ROAS/LTV + lý do chưa chốt gộp lead thua + cơ hội mất; đơn hàng; doanh thu tách bán mới/mua lại; mua lại + chiến dịch; công việc theo loại); menu thêm mục **Báo cáo**. Kiểm `scripts/thu_b11.py` **46/46 PASS** (DB có dữ liệu THẬT nên số toàn cục kiểm ≥, số theo-người kiểm ĐÚNG tuyệt đối); hồi quy 19 bộ **712/712 xanh** → tổng **758 PASS / 20 bộ**. *Còn nợ ngoài B: màn 63 + điểm AI (C-MVP3); màn 8/11/21 bộ lọc nâng cao; màn 42-46 chờ nhập danh mục thật. Kế: **Phần C** (tổng đài → kho kiến thức → Ads mở rộng) — cần tài khoản tổng đài từ user* |
| 01/08/2026 | **B10 XONG — Mua lại & khách ngủ** (FR-120…123): DB thêm 3 cột `repurchase_opportunities` (readiness · lost_note · stage_moved_at — bảng + `reactivation_*` có sẵn từ đầu). **FR-122 chốt thiết kế:** 9 trạng thái đặc tả phần lớn là trạng thái THEO THỜI GIAN → suy từ `expected_close_date` lúc đọc (`trang_thai_hien_thi`), cột `stage` chỉ giữ bước do người làm (identified→contacted→negotiating→won/lost/postponed, TRANSITIONS chặn đi lùi); 'lost' bắt buộc lý do — mã chuẩn 9 lý do BRD tra `lead_reasons`, REPURCHASE-006. **FR-120:** ngày hết = bắt đầu THẬT (care plan B9 → start_date → ngày giao) + số ngày (mẫu hoặc số lượng × ngày/đơn vị) × hệ số tuân thủ đọc từ phiếu chăm (Thiếu liều ×1.25 · Ngắt quãng ×1.5 · Chưa dùng chặn) + tạm dừng + hàng cũ; trả breakdown minh bạch, lưu `customer_treatments.expected_end_date` + đồng bộ cơ hội mở (nguồn số thông báo đến hạn màn 3). **FR-123:** khách ngủ = từng mua mà im ≥30/60/90/180 ngày (loại do_not_contact ngay trong SQL), lọc tổng giá trị; gán chiến dịch `reactivation_campaigns/members` (chọn sẵn/tạo mới, khách trùng bỏ qua theo unique) + mỗi khách 1 việc `mua_lai`; **doanh thu tái kích hoạt đo TỰ ĐỘNG** — hook order_service khi giao TC: cơ hội mở → 'won' (Đã mua = có đơn mới) + member chiến dịch running → 'converted', báo cáo cộng đơn giao TC TẠO SAU khi vào chiến dịch. 12 route API (10 mã REPURCHASE + 2 chiến dịch), **include TRƯỚC customers router** vì `/customers/sleeping` là literal phải thắng `/customers/{id}`. Màn 39-40 (`/crm/mua-lai` — 9 nhãn + chuyển bước trên dòng) + màn 41 (`/crm/khach-ngu` — rổ ngưỡng, tick gán chiến dịch, bảng doanh thu) + menu 😴. Kiểm `scripts/thu_b10.py` **40/40 PASS**; hồi quy 18 bộ **672/672 xanh** (tổng **712**, 19 bộ). Bước kế: **B11 — Báo cáo + trả nợ màn hình** |
| 01/08/2026 | **MÀN 9 · 10 · 22 XONG — trả nợ giao diện cho phần backend đã có** (làm theo [TIEN-DO-MAN-HINH.md](TIEN-DO-MAN-HINH.md), không đụng lát mới). **Màn 9 hồ sơ khách 360°** `/crm/khach-hang/{id}` — đủ 9 khu vực PDF thành 9 tab, mỗi tab nạp riêng (mở hồ sơ không bắn 15 query): Tổng quan (chi tiêu · lead · việc tiếp · **cảnh báo an toàn đỏ**) · Hội thoại (danh sách + **khung chat đọc `crm.messages`**, chưa kéo tin thì chỉ đường bật MSG_SYNC) · Cuộc gọi (nói rõ thuộc C-MVP3 chứ không để trống) · Hồ sơ tư vấn (triệu chứng · nội soi · thuốc · sàng lọc · ca chuyên môn) · Liệu trình (items snapshot + đề xuất B6) · Đơn hàng · Chăm sóc (phiếu bàn giao + mốc CS + cơ hội mua lại) · Marketing (quy nguồn + UTM) · Lịch sử thay đổi (audit gom cả lead/đơn/phiếu của khách). **Màn 10 gộp trùng** `/crm/khach-hang/gop-trung` — dò SĐT **dùng lại `find_duplicate_groups` của B1** (không viết truy vấn thứ hai để hai chỗ khỏi lệch định nghĩa "khách sống") + bù nhánh **Facebook ID** mà đặc tả đòi; chọn hồ sơ chính → gộp, hồ sơ phụ `merged` không xoá. **Màn 22 chi tiết đơn** `/crm/don-hang/{id}` — hàng · tiền · địa chỉ/vận chuyển moi từ `pos_raw` (không tự dịch nghĩa) · lịch sử trạng thái · việc + liệu trình liên quan · quy nguồn · nút mở phiếu bàn giao. Màn 8 và 21 nay bấm được vào chi tiết; bỏ dải "màn khung" ở hai màn đó. Repo mới `profile_repo.py` (chỉ JOIN, không luật). Kiểm mới `scripts/thu_man_hinh.py` **40/40 PASS** (kiểm dữ liệu LÊN ĐÚNG TAB chứ không chỉ 200); hồi quy 17 bộ **632/632 xanh** (tổng **672**). Màn hình: **34 ✅ · 11 🔨 · 35 ⬜**; MVP **9 ✅ · 6 🔨 · 3 ⬜** |
| 01/08/2026 | **B9 XONG — Chăm sóc 11 bước** (FR-100…110): DB 74→**76 bảng** (`no_response_sequences` + `no_response_attempts`; `care_plans` +actual_start_date/cskh_state/cycle_no; `care_plan_steps` +data jsonb/note/completed_by + unique (plan, mã mốc); `customers` +do_not_contact). **FR-102 là trục**: mốc CS04-08 sinh ĐÚNG ngày 4/10/15/20/25 từ `actual_start_date` (CS03 ghi) — chưa bắt đầu dùng là KHÔNG sinh mốc đánh giá. Phiếu 11 bước (CARE-STEP-001…011) đi MỘT cửa `care_service.ghi_phieu`: trường bắt buộc đọc `ref_codes` bảng 18, giá trị chuẩn 7 bộ bảng 19 (thêm trường = seed, khỏi sửa code); không gặp khách (`contact_result` ≠ Kết nối) chỉ ghi tương tác — mốc không đóng. Automation cứng: AU02 (CS01 3 lần không gặp → việc khẩn báo Sale), AU04 (nhận đủ → CS03 +2d), AU05, AU06/07 (phản ứng Vừa/Nặng · RS05/06 · RS04+dùng đúng → ca chuyên môn B5 + C05), AU08 (CS07 → cơ hội mua lại, đang C05 không đè cột), AU09 (CS08 chưa mua → BẮT lý do + CS09 đúng ngày 28), AU11 (do_not_contact → C09 + mốc chờ skipped + đóng chuỗi — mọi phiếu/worker chặn từ SQL). Đánh giá ngày 15: ASSESSMENT-001…003, `before` mặc định lấy điểm NỀN B5 (`customer_symptoms.severity`), change_score cột sinh tự động. Chuỗi không phản hồi FR-110: đúng thứ tự nhắn→gọi→nhắn→gọi (sai kênh chặn), Kết nối → responded, đủ 4 → lost_contact + C08; 1 khách 1 chuỗi active (unique partial). Worker `care-steps` 5'/lần: pending→due + việc `cham_soc` idempotent cho owner. 18 route `/api/v1/care*` phủ 28 mã API (đọc customer.view · ghi customer.edit · điểm triệu chứng health.view); B8 hook gọi `khoi_tao_moc` (CS01+CS02 ngay khi bàn giao). Màn 27 THẬT (kanban C01-C09 đếm `cskh_state`) + màn 28-37 gộp một màn `/crm/cham-soc/{id}` (phiếu ĐỘNG theo danh mục, bỏ qua mốc đòi lý do). Kiểm `scripts/thu_b9.py` **57/57 PASS**; hồi quy 16 bộ **568/568 xanh** (tổng **625**). *Bổ sung ngay sau đó: **màn 38 lên web trọn** (khối 📵 trong màn kế hoạch: mở chuỗi, kênh tự ép đúng thứ tự, đóng chuỗi, nút ⛔ ngừng liên hệ) — thu_b9 nâng **64/64** (tổng **632**). Thông báo `khach_phan_ung` nối notify khi B10 đổ nguồn. Bước kế: **B10 — Mua lại & khách ngủ*** |
| 01/08/2026 | **MÀN 3 XONG — Trung tâm thông báo (NOTIFY-001…004)**: DB 72→**74 bảng** (`notifications` + `notification_settings`). **Kiến trúc cố ý:** thông báo KHÔNG sinh trong service nghiệp vụ mà do worker `notify-scan` **QUÉT DB** 11 nguồn (lead mới · việc sắp đến hạn · việc quá hạn · khách cần gọi lại · khách có phản ứng · cần chuyển chuyên môn · đơn giao thành công · đơn hoàn · đến hạn mua lại · nội dung chờ duyệt · lỗi đồng bộ) — thêm/bớt loại chỉ sửa `notification_service.py`, KHÔNG đụng luật B1…B8 và không có nguy cơ vỡ luồng bán vì một câu thông báo; mỗi nguồn tự nuốt lỗi (1 nguồn hỏng không kéo sập 10 nguồn kia) và có TRẦN 200 dòng/lượt (DB đang có sẵn hàng trăm việc quá hạn cũng không dội). `dedupe_key` unique theo (người nhận, sự việc) → quét lại không trùng, **đã đọc rồi thì không réo lại**. Người nhận: bản ghi có người phụ trách thì gửi đúng người; ca lâm sàng chưa gán → vai trò *Người chuyên môn* (KHÔNG dùng quyền `health.view` vì Sale/CSKH cũng có → báo cả công ty); nội dung chờ duyệt → `content.approve`; lỗi đồng bộ → `integration.manage` và CHỈ báo dòng `given_up` hoặc đã thử ≥3 lần (lỗi mạng thoáng qua worker tự chữa). NOTIFY-004 mỗi người tự tắt loại không muốn (bảng chỉ giữ phần đã đổi, thiếu dòng = bật). Web: màn `/crm/thong-bao` (chips lọc theo loại + số chưa đọc, đánh dấu đã đọc từng cái/tất cả, khối cài đặt) + **chuông + huy hiệu số chưa đọc trên mọi trang** (cache 20s/người, DB lỗi thì chuông im chứ không vỡ trang). 3 công tắc `notify_*` trên màn Cài đặt (mặc định **BẬT** — khác đồng bộ Pancake vì chỉ nhặt sự việc đã có, không sinh dữ liệu nghiệp vụ); tự dọn thông báo ĐÃ ĐỌC quá 60 ngày, chưa đọc thì không bao giờ xoá. Kiểm `scripts/thu_thong_bao.py` **39/39 PASS**; hồi quy 15 bộ **529/529 xanh** (tổng 568) |
| 01/08/2026 | **B8 XONG — Bàn giao Sale→CSKH** (FR-090/091): DB không thêm bảng (handovers + care_plans có sẵn từ đầu). FR-090 tự động: hook trong `order_service.change_status` khi delivered/collected (cả chuyển tay lẫn POS force) — tạo phiếu idempotent (uq_handovers_order, automation đua nhau không trùng), gán CSKH **vòng tròn theo tải** (`handover_repo.cskh_it_viec` — khách có CSKH sẵn thì giữ nguyên), ghi Sale chốt từ `customer_assignments`, chép liệu trình (tên mẫu + cách dùng từ items snapshot B6) + mồi triệu chứng/thuốc/bệnh nền từ hồ sơ B5, tạo vỏ `care_plans` (mốc CS01-11 chờ B9) + việc onboarding (task engine B4), ngày dự kiến bắt đầu = giao + 2 ngày (nhịp CS03). FR-091: 8 trường bắt buộc (`BAT_BUOC`) — thiếu → is_complete=false + missing_fields; **nhận đòi hồ sơ ĐỦ**; trả lại đòi lý do + sinh việc `xu_ly_su_co` cho Sale kèm danh sách thiếu; Sale bổ sung đủ → phiếu returned TỰ quay lại assigned; nhận xong mở `customer_assignments` vai cskh + care_plans.owner theo người nhận; đã accepted thì cấm sửa người (đi đường chuyển khách A5). 8 API `/api/v1/handovers*` (quyền customer.view/edit) + **màn 24-25 làm THẬT** (`/crm/ban-giao` — chips lọc, 8 trường thiếu tô đỏ, nút Nhận/Trả/Gán; menu mục Bàn giao). pos_sync: đóng `delivered_at` TRƯỚC change_status (phiếu có ngày), đơn về đã-giao cũng hook, `run_type=backfill` thì TẮT hook (53k đơn cũ không sinh 53k việc ảo). Kiểm `scripts/thu_b8.py` **41/41 PASS**; hồi quy 14 bộ **488/488 xanh** (tổng 529). Vá kèm 3 test cũ FAIL OAN vì user đã BẬT đồng bộ thật (90 lead + doanh thu thật vào DB): thu_b3 limit 50→5000, thu_muc4 top 5→1000, thu_quang_cao `==`→`>=`. Bước kế: **B9 — Chăm sóc 11 bước** |
| 01/08/2026 | **FR-012 TRỌN VẸN — tin nhắn về `crm.messages` + API hội thoại CONV-001…006** (đóng nốt ~10% mục III Tích hợp Pancake): DB 72 bảng giữ nguyên, `messages` +4 cột (sender_external_id/sender_name/msg_type/attachments) + unique (conversation_id, external_message_id), `conversations` +messages_synced_at; sync_logs/sync_errors nhận entity `message`. `integrations/pancake/message_sync.py` kéo theo độ tươi (chỉ hội thoại có tin mới), lỗi GỌI API tự lành theo mốc, lỗi GHI DB vào hàng đợi retry phát lại từ payload (thêm nhánh `message` trong `chay_hang_doi`); worker `msg-sync` + 3 cài đặt `msg_sync_*` trên màn Cài đặt (22 mục, mặc định TẮT); luật FR-012 "không sửa nội dung gốc" = upsert DO NOTHING. 7 API `/api/v1/conversations*` (CONV-001…006 + PANCAKE-010 external-link), quyền đọc `customer.view` / ghi `customer.edit`, CONV-006 gửi thật có audit + ghi luôn bản CRM. Backfill tay: `scripts/backfill_tin_nhan.py` (nghỉ nhịp chống 429, bỏ qua conv lỗi trong phiên). Kiểm mới `scripts/thu_conv.py` **33/33 PASS**; hồi quy 13 bộ **455/455 xanh** (tổng 488). *Màn hiển thị tab Hội thoại (màn 9) chờ B11; webhook vẫn chờ domain. Bước kế: **B8 — Bàn giao Sale→CSKH*** |
| 01/08/2026 | **MÀN 2 XONG — Trang chủ theo vai trò** (danh sách màn hình mục I): route `/crm/trang-chu`, "/" + đăng nhập xong vào thẳng đây (FR-001 "dashboard theo vai trò"); menu thêm mục Trang chủ. 9 vai trò mỗi vai một bộ số + lối tắt riêng: Sale/CSKH thấy CỦA MÌNH, trưởng nhóm thấy CẢ ĐỘI (tra `teams.manager_id` trong DB, không tin token) + hàng đợi lead chưa nhận + bảng tải theo nhân viên; Marketing chi phí 7/30n + ROAS; Kế toán đơn theo trạng thái + doanh thu ngày/tháng; Người chuyên môn ca chờ + đề xuất chờ duyệt + khách cờ đỏ/vàng; Admin lỗi đồng bộ + phiên + audit; Chủ DN toàn cảnh + ads. Vai trò LẠ rơi về bản "khac" không vỡ trang; khối phụ thuộc B8/B9 ghi chú ngay dưới, số trung thực = 0. Kiểm mới `scripts/thu_trang_chu.py` **19/19 PASS**; hồi quy 12 bộ cũ **436/436 xanh** (tổng 455) |
| 31/07/2026 | Đọc 3 PDF đặc tả; tái cấu trúc thư mục app (core/ai/web/integrations/db-repositories), route giữ nguyên 43/43; chuyển tài liệu vào `docs/` |
| 31/07/2026 | Dựng `scripts/init_crm.sql` và nạp vào Postgres — schema `crm` 58 bảng · 99 FK; sinh ERD mermaid; thêm BRD chi tiết |
| 31/07/2026 | Viết `THU-TU-TRIEN-KHAI-CRM.md` (lộ trình A→B→C) và file tiến độ này. Bước kế: **A2 — Login** |
| 31/07/2026 | Viết mô tả A2 (`A2-DANG-NHAP.md`): 6 API auth, khoá 37 route web bằng 1 middleware, sửa `users` + bảng `user_sessions`, seed 9 vai trò + 11 quyền. Chờ duyệt |
| 31/07/2026 | **A2 XONG** — schema 59 bảng (`users` +3 cột, thêm `user_sessions`); `core/security` + `services/auth_service` + `api/v1/auth` + màn `/dang-nhap` + middleware khoá; seed 9 vai trò · 11 quyền · admin; tắt `/docs`; nghiệm thu **27/27 PASS** (khoá 5 lần sai, thu hồi phiên, audit có IP, worker chạy như cũ). Bước kế: **A3** |
| 31/07/2026 | Đọc BRD mục 14 → seed danh mục (`scripts/seed_danh_muc.py`, chạy lại được): pipeline Bán mới 13 giai đoạn, 9 lý do chưa mua, bảng mới `ref_codes` 78 mã (C01-C09 · CS01-CS11 · RS01-RS12 · AU01-AU13 · 7 bộ giá trị phiếu chăm). **Sửa CHECK `care_plan_steps.step_code`**: bộ D1/D3/D7 đoán trước đây SAI, BRD chuẩn là CS01-CS11 mốc 4/10/15/20/25/28 tính từ ngày BẮT ĐẦU DÙNG thật. DB nay 60 bảng, ERD sinh lại khớp 60/60 |
| 31/07/2026 | **A3+A4+A5 XONG** — khung API chuẩn (`core/errors` + `core/response` + `require_permission` + exception handler chung, auth refactor theo); audit mở rộng (list/get + API + màn 77, ghi CHỈ trường đổi); quản lý nhân viên đủ USER/ROLE/PERMISSION/TEAM + reset MK + chuyển khách (customer_assignments đóng dòng cũ, giữ lịch sử) + luật FR-002 chặn khoá khi còn giữ khách; web 3 màn Quản trị + menu ẩn theo quyền; quyền 11→13. Nghiệm thu **30/30 PASS**, chạy lại bộ A2 **27/27**. Bước kế: **B1 — Khách hàng 360°** |
| 31/07/2026 | **B3 tầng luật xong** — `lead_repo` + `lead_service` (chia vòng tròn theo tải, 5 luật chặn FR-040, lịch sử FR-041, SLA 5'/15', hàng đợi, quá hạn, nóng); `leads` +5 cột, `lead_stage_history` +note; sửa trigger `check_lead_stage_pipeline` thiếu tiền tố `crm.` (vỡ khi gọi từ app); kiểm `scripts/thu_b3.py` **25/25 PASS**. API LEAD-001…011 chờ A3 nối |
| 31/07/2026 | **B3 XONG (trừ Kanban)** — nối API lên khung A3: `api/v1/leads.py` 15 endpoint (PIPELINE-001…004 · LEAD-001…011 · +/leads/queue), quyền customer.view/edit + user.manage cho cấu trúc pipeline; kiểm `scripts/thu_b3_api.py` **16/16 PASS**. Vá kèm A2: cột inet nhận chuỗi không phải IP → login 500 + audit rơi lặng lẽ, nay lọc ở biên repo (`_ip_hop_le`). Bước kế: **B1 — Khách hàng 360°** |
| 31/07/2026 | **B1 XONG (trừ màn)** — chuẩn hoá SĐT (`services/phone.py`); chống trùng 4 bậc FR-011 (SĐT ra nhiều khách thì KHÔNG tự nhận — nhà chung số); `upsert_from_source()` chờ sẵn cho B2, đồng bộ lại không trùng khách/lead; gộp FR-022 dồn 20 bảng 1 transaction, hồ sơ phụ status=merged không xoá; xoá mềm; tag tìm-hoặc-tạo; phân công 3 vai riêng có lịch sử; 14 API + schema `customers` +3 cột +unique external. Kiểm `scripts/thu_b1.py` **38/38 PASS**. Bước kế: **B2** |
| 31/07/2026 | **B2 XONG** — `integrations/pancake/crm_sync.py`: mỗi hội thoại poller → khách (4 bậc chống trùng) + định danh + `crm.conversations` + lead tự động; tự tạo `crm.pages`; lỗi nuốt từng dòng, không vỡ luồng bot. Công tắc `CRM_SYNC_ENABLED` mặc định **TẮT** (bật mới sinh dữ liệu thật); kho cũ 1.5k hội thoại: `scripts/backfill_crm_tu_watcher.py` (idempotent). Kiểm `scripts/thu_b2.py` **11/11 PASS**; chạy lại cả 4 bộ B1/B2/B3/B3-API đều xanh (90 PASS tổng). Bước kế: **B4 — Công việc** |
| 31/07/2026 | Merge 2 máy (zz + wip thẻ Pancake) không xung đột, 90/90 vẫn xanh, đã push `4544203`. Seed đủ 11 tài khoản 9 cấp bậc (`scripts/seed_tai_khoan_mau.py` — Sale/CSKH mỗi vai 2 người cho chia vòng tròn; 2 đội có trưởng nhóm); kiểm đăng nhập + biên quyền từng cấp **13/13 PASS**. Mật khẩu in một lần lúc seed, cấp lại: `--reset-passwords` |
| 31/07/2026 | **B6 XONG** — sản phẩm & liệu trình + rule engine (FR-060…062): DB 65 bảng (thêm `treatment_recommendations`; `product_versions` +cột price). FR-060 ở `product_service`: KHÔNG có delete, đổi giá tự snapshot phiên bản (đơn cũ giữ giá cũ), nội dung mới → approval về `pending` (chưa duyệt không được tư vấn dùng, duyệt = `content.approve`). FR-061/062 ở `treatment_service`: luật nằm TRONG DB `treatment_rules` (mục 10 BRD — condition_json 4 kiểu: screening / symptom+min_severity / symptom_group / safety_flag; rule_type exclusion·suitable·warning, định dạng được validate lúc nạp); engine đề xuất: cờ đỏ chặn (gọi `kiem_duoc_de_xuat` B5), chưa khai triệu chứng chặn, exclusion loại kèm lý do, suitable phải khớp mới hiện, warning/cờ vàng → đề xuất `pending_approval` PHẢI chuyên môn duyệt mới tạo được liệu trình; tạo liệu trình SNAPSHOT items từ mẫu; điều chỉnh (TREATMENT-014) đòi lý do + audit, kết thúc rồi thì khoá. 22 API (products.py + treatments.py; danh mục = `user.manage`, đề xuất/liệu trình = `treatment.edit`, duyệt = `content.approve`). Kiểm `scripts/thu_b6.py` **43/43 PASS** (đủ tiêu chí "bệnh nền trong loại trừ → mẫu không hiện"); regression 7 bộ **199/199 xanh** (tổng 242). *Màn 42-46 chờ khung B11. Bước kế: **B7 — Đơn hàng** (NHỚ: phải dò API Pancake POS trước — xem Việc lẻ)*|
| 31/07/2026 | **B5 XONG** — hồ sơ tư vấn + sàng lọc an toàn (FR-050…053): DB 60→64 bảng (thêm `examinations`, `current_medications`, `previous_treatments`, `clinical_escalations`; `customer_symptoms` +3 cột occurs_when/meal_relation/note; `customers.safety_flag`; `safety_screenings` +cleared_at/by — gỡ phiếu GIỮ VẾT không delete); seed 19 triệu chứng tiêu hoá vào `symptoms`. Luật ở `consult_service`: FR-050 ghi chú KHÔNG thay dữ liệu cấu trúc; FR-052 thuốc có phản ứng → tự mở ca; **FR-053** 11 mục sàng lọc — 6 red flag (nôn máu/phân đen/nuốt nghẹn/đau ngực/khó thở/sụt cân) → cờ ĐỎ + `kiem_duoc_de_xuat()` chặn đề xuất (B6 PHẢI gọi) + ca `clinical_escalations` kèm task `duyet_chuyen_mon` khẩn (B4) giao Người chuyên môn, không mở ca trùng; 5 mục yellow → cờ vàng không chặn; gỡ cờ CHỈ qua SAFETY-005 resolve (quyền `content.approve`), phiên tư vấn chặn hoàn tất khi thiếu 7 câu bắt buộc. 17 API (5 CONSULT · 3+1 SYMPTOM · 4 MEDICAL · 5 SAFETY), quyền `health.view`. Kiểm `scripts/thu_b5.py` **37/37 PASS**; regression 6 bộ **162/162 xanh** (tổng 199). *Màn 13/14 chờ khung B11. Bước kế: **B6 — Sản phẩm & liệu trình***|
| 31/07/2026 | **B4 XONG** — task engine chung Sale/CSKH (mục 19 BRD): `tasks` +5 cột (title/result/created_by/completed_at/escalated_at); `task_repo` + `task_service` (8 loại việc chuẩn; owner + due_at BẮT BUỘC; **không đóng thiếu kết quả** — update status=done bị chặn, phải qua /complete; huỷ/dời lịch/chuyển người đều đòi lý do; liên kết đa hình tự kiểm bản ghi đích); API `api/v1/tasks.py` 9 endpoint TASK-001…009 (today/overdue khai trước {id}); worker `tasks_qua_han` 5'/lần đánh dấu escalated_at + audit `task_escalated` (báo quản lý theo SLA — dời lịch thì xoá dấu, quét lại từ đầu); màn `/crm/cong-viec` đổ dữ liệu thật, mặc định "việc của tôi", `?pham_vi=tatca` xem cả đội, khối quá hạn đỏ. Kiểm `scripts/thu_b4.py` **41/41 PASS**; regression 5 bộ cũ **121/121 xanh** (b1 38 · b2 11 · b3 25 · b3api 16 · quản trị đội 31). *Hoãn sang lát sau: task_templates theo pipeline/care plan (B8/B9), automation builder Khi–Thì (C). Bước kế: **B5 — Hồ sơ tư vấn*** |
| 31/07/2026 | **Khoá khu Bot Pancake theo cấp** — quyền mới `bot.view` (nay 15 quyền), seed CHỈ cấp qua `_ALL` cho Chủ DN + Admin; middleware `auth_gate` chặn theo tiền tố `_KHU_BOT` (/bang-dieu-khien, /tin-nhan, /khach-hang, /cam-xuc, /data, /pancake — ăn trọn fragment/form con), thiếu quyền trả trang 403; nhóm menu Bot Pancake ẩn với cấp dưới (sidebar bỏ luôn tên nhóm rỗng); `render_403` dùng chung được (thêm heading + message riêng). Màn CRM /crm/* và /quan-tri không đổi. Kiểm `thu_quan_tri_doi` **31/31 PASS** (+9 ca mới), `thu_b3_api` 16/16 |
| 31/07/2026 | **Màn Nhân viên tìm + lọc kiểu Pancake** — ô tìm "Tìm tên, @tài khoản, SĐT…" (gõ `@` dò riêng username; SĐT trúng cả khi gõ liền dù DB lưu có khoảng cách); dải chip lọc theo nhóm ĐỘNG từ `crm.teams` (Tất cả · từng nhóm · Chưa vào nhóm — tạo nhóm mới là chip tự hiện, không sửa code); bảng thêm cột SĐT, form tạo tài khoản thêm Điện thoại; nới CHECK `teams.department` thêm `kho_van` (Kho vận) ở DB + `init_crm.sql` + dropdown tạo nhóm. Smoke 9/9 PASS, `thu_quan_tri_doi` 31/31 |
| 31/07/2026 | **Toolbar Nhân viên một hàng (theo mẫu user đưa)** — 🔍 ô tìm + chip nhóm + ⬇ Xuất Excel nằm chung 1 dải (`um-toolbar`); lọc chuyển sang chạy NGAY trên trình duyệt (`_UM_JS`, gõ là lọc, bấm chip là lọc, không tải lại trang — chạy lại sau PJAX nhờ `data-page-script`); tiêu đề nhóm tự ẩn khi nhóm bị lọc hết; `?q=`/`?nhom=` trên URL vẫn ăn (deep-link, chọn sẵn chip). **Xuất Excel mới**: `GET /quan-tri/nhan-vien/xuat-excel` — CSV BOM UTF-8 + chấm phẩy (Excel VN mở thẳng), đòi thêm quyền `data.export`, mỗi lần xuất ghi audit `users_export`; trưởng nhóm không có quyền là ẩn nút + 403. Smoke 17/17 PASS, `thu_quan_tri_doi` 31/31 |
| 31/07/2026 | **Vá đăng xuất** — bấm Đăng xuất xong màn hình đứng nguyên (PJAX swap không tìm thấy `.side`/`.main` vì màn đăng nhập là trang độc lập → lặng lẽ bỏ qua), phải tự F5 mới thấy màn đăng nhập. Nay `_NAV_JS` gặp trang ngoài shell là điều hướng thật (`location.replace`) — ăn luôn cả trường hợp phiên hết hạn giữa chừng; kèm `Cache-Control: no-store` cho mọi trang sau đăng nhập (đăng xuất xong bấm Back không moi lại nội dung cũ từ bfcache). Smoke luồng login→logout→302 + 2 bộ TestClient (22+16) xanh |
| 31/07/2026 | **Phân cấp tạo tài khoản (mở rộng A5)** — quyền mới `user.manage_team` (nay 14 quyền, seed gán cho 2 trưởng nhóm): Chủ DN/Admin (`user.manage`) tạo mọi cấp như cũ; trưởng nhóm Sale/CSKH chỉ THẤY đội mình, chỉ TẠO + reset MK cho vai trò thành viên (Sale/CSKH) — vai trò + đội bị ÉP trong service (`user_service.pham_vi_doi`: tra DB teams.manager_id + department, không tin token); sửa/khoá/chuyển khách vẫn riêng Admin. Màn `/quan-tri/nhan-vien` tự thu gọn theo quyền (hết nút khoá, hết link hồ sơ, vai trò cố định), menu Quản trị hiện cho trưởng nhóm; API USER-001/003 + reset mở cho `user.manage_team`. Bảng nhân viên gom theo đội cho dễ nhìn (mỗi đội một dải tiêu đề, trưởng nhóm đứng đầu, chưa vào nhóm xuống cuối — bỏ cột Nhóm). Kiểm mới `scripts/thu_quan_tri_doi.py` **22/22 PASS**. Vá kèm `thu_b3`: từ khi seed sale01/02, vòng tròn chia lead cho họ (0 lead, id nhỏ) làm test lệch — nay tạm khoá Sale thật trong lúc chạy, mở lại trong finally; 4 bộ cũ chạy lại **90/90 xanh** |
| 31/07/2026 | **Bộ màn CRM khung (tạm)** — 8 màn `/crm/*`: Tổng quan (màn 4) · Khách hàng (8) · Pipeline Kanban 13 cột (11) · Công việc (12+26) · Đơn hàng (21) · Chăm sóc C01-C09 (26-27) · Mua lại (39-40) · Sản phẩm & liệu trình (42+44). Số liệu ĐẾM THẬT từ schema `crm`, mỗi màn ghi rõ lát cắt nào (B1…B11) đổ dữ liệu; menu chia 2 nhóm **CRM / Bot Pancake**, đăng nhập xong vào thẳng `/crm/tong-quan` (màn bot cũ giữ nguyên URL). Smoke 8/8 · regression A2 27/27 + A3-5 30/30 |
| 31/07/2026 | **Đối chiếu lại 4 file đặc tả** trong `docs/` (BRD .docx + 3 PDF: FR-001…182 · 80 màn hình · ~230 API). Kết quả: A1-A5 + B1-B4 khớp đúng mã đặc tả (B1=14 API CUSTOMER/IDENTITY, B3=LEAD+PIPELINE, B4=TASK-001…009, A5=USER/ROLE/PERMISSION/TEAM). Bổ sung tham chiếu FR · mã API · số màn vào từng lát B5-B11 + C-MVP3/4/5 để mở đúng nhóm khi làm. Phát hiện 5 nhóm đặc tả chưa thuộc lát nào → ghi vào Việc lẻ: FILE upload (B5 cần), CONV API đọc hội thoại, Trung tâm thông báo (NOTIFY), quản lý danh mục MASTER (màn 72), SYSTEM/BACKUP (màn 78-80). Bước kế vẫn là: **B5 — Hồ sơ tư vấn** |
| 31/07/2026 | **Menu trái: khối Sale xổ/thu (kiểu Kallet, theo mẫu user đưa)** — mục Pipeline Sale phẳng nâng thành <details> Sale: Nhiệm vụ (🎯 Bảng chăm sóc) → Cột trên bảng (13 giai đoạn, chấm màu cố định + số lead đang mở, cache 15s vì sidebar vẽ mọi trang — crm_screens_repo.sale_menu) → Công cụ (Thư viện kịch bản chỉ hiện khi có bot.view · Bảng giá & liệu trình); bấm cột nào là mở Kanban /crm/pipeline?st= tô sáng + cuộn tới đúng cột đó; đang đứng ở pipeline thì khối tự mở; DB lỗi lùi về link phẳng. CSS nd-* viết chung cho cả khối CSKH của phiên song song; nút Xuất Excel thêm attr download để PJAX nhường trình duyệt. Smoke 11/11 + thu_quan_tri_doi 31/31 + smoke nhân viên 17/17 |
| 31/07/2026 | **Rà DB theo code mới (B5/B6)** — nạp thử init_crm.sql vào DB tạm rồi diff với DB thật: cột khớp 100%, seed danh mục đủ (78 ref_codes · 13 giai đoạn · 9 lý do). Sửa 3 chỗ: (1) init_crm.sql vỡ trên DB MỚI TINH vì clinical_escalations khai FK inline sang tasks nhưng tasks tạo sau → chuyển thành add constraint sau MODULE 6; (2) DB thật thiếu CHECK ck_leads_temperature (nong/am/lanh — 7 lead hiện có đều NULL, an toàn) → đã thêm cả DB lẫn khối nâng-cấp-DB-cũ trong file; (3) đổi tên 2 FK tự đặt (handovers_care_plan_id_fkey, clinical_escalations_task_id_fkey) về tên trong file để chạy lại không sinh FK trùng. Chạy lại init_crm.sql trên DB thật: 0 lỗi, 0 FK trùng, ràng buộc khớp 100%; hồi quy thu_b3 25/25 + thu_b5 37/37 + thu_b6 43/43. Lưu ý: products/treatment_templates/treatment_rules đang 0 dòng — B6 chờ nhập danh mục thật (chưa có seed) |
| 01/08/2026 | **CÀI ĐẶT TRÊN WEB XONG (màn 78 · SYSTEM-001/002)** — gom **19 công tắc + nhịp chạy** đang nằm rải rác trong `.env` vào một màn `/quan-tri/cai-dat` (3 nhóm: Đồng bộ · Quảng cáo · Bot Pancake). DB 71→**72 bảng**: `app_settings` CHỈ giữ phần người đã đổi — nhãn/kiểu/khoảng hợp lệ/mặc định nằm trong code (`app/core/runtime_config.py` danh mục `MUC`), nên thêm một cài đặt mới = thêm một dòng Python, không phải migration; xoá dòng trong bảng là quay về đúng .env. Thứ tự ưu tiên `app_settings → .env → mặc định`, cache 10 giây. **Worker nay đọc lại công tắc/nhịp MỖI VÒNG** (poller · cảm xúc · pos_orders · ads_cost · sync_retry) thay vì đọc-một-lần lúc khởi động, và `lifespan` LUÔN tạo đủ vòng lặp (trước đây tắt trong .env là không tạo task → bật trên web cũng vô nghĩa) — chỉ 2 worker POS còn điều kiện, mà đó là điều kiện CẤU HÌNH (chưa có api_key thì bật cũng không gọi được). Công tắc cảm xúc dùng CHUNG file switch cũ, không đẻ nguồn sự thật thứ hai. Luật: sai một ô thì cả nhóm không ghi (tránh nửa bật nửa tắt); checkbox không tick = TẮT (trình duyệt không gửi ô đó lên — chỗ này rất dễ sai); mọi thay đổi vào audit; **token/mật khẩu/chuỗi kết nối CỐ Ý không bày lên web**. API SYSTEM-001/002 + `PUT /settings` (lưu cả mẻ) + `POST /settings/{code}/mac-dinh`, quyền `user.manage`. Màn Tích hợp và màn Quảng cáo nay trỏ thẳng sang đây thay vì bảo người dùng đi sửa .env. Kiểm `scripts/thu_cai_dat.py` **39/39 PASS**; hồi quy 11 bộ **397/397 xanh** (tổng 436) |
| 01/08/2026 | **NGUỒN QUẢNG CÁO XONG (mục 4 phần Ads)** — **Phát hiện lớn:** Pancake POS có sẵn **Ads Manager API** (`shops/{id}/ads_manager/ad_accounts·campaigns_v2·ad_sets_v2·ads_v2`), mỗi dòng kèm `insights` (spend/impressions/clicks/reach/cpc/cpm/ctr) → lấy được CẢ CÂY QUẢNG CÁO LẪN CHI PHÍ mà **không cần Facebook Ads API** (ADS-001 "kết nối tài khoản QC" của đặc tả coi như xong bằng đường này, đỡ giữ thêm 1 token). DB 70→**71 bảng**: `ad_metrics_daily` (chi phí/chỉ số theo NGÀY — hạt mịn nhất, cộng ra được mọi cửa sổ 7/30/60/90 của ADS-010; API chỉ trả số đã tổng hợp theo khoảng nên phải hỏi từng ngày); `ad_campaigns` +9 cột (tài khoản, mục tiêu, ngân sách, mốc), `ad_sets` +10 (targeting, optimization_goal…, campaign_id cho NULL), `ads` +6 (creative_name, object_story_id → cắt ra post_id khớp bài viết trên đơn); CHECK status hạ về 5 nhóm chuẩn hoá (Facebook trả cả chục trạng thái), sync_logs/sync_errors nhận thêm entity `ad`. Code: `client` +4 hàm ads_manager (tự lật trang) · `ads_sync` (dong_bo_cay / dong_bo_chi_phi, `_luu_ad` dựng cả cây từ 1 dòng ads_v2) · `ads_repo` (upsert cây + `noi_attribution_vao_ads` để đơn-về-trước-cây-về-sau vẫn khớp + 3 báo cáo dùng CHUNG một khối tính, không để 3 nơi tính 3 kiểu) · `ads_service` · worker `ads_cost` (6h/lượt, làm tươi hôm nay + hôm qua vì FB chốt số muộn; `ADS_SYNC_ENABLED` mặc định TẮT) · `scripts/backfill_quang_cao.py`. Web: `/crm/quang-cao` 3 tab (Chiến dịch·Nhóm·Quảng cáo) + phiếu sức khỏe 1 ad (phễu·lý do chưa chốt·khách minh chứng), quyền mới **`ads.view`** (18 quyền; Marketing được cấp). API 12 endpoint (ADS-002…008·010 · ATTRIBUTION-001/002 · FUNNEL-004). **Luật hiển thị:** ad chưa nối tài khoản QC → chi phí/ROAS để **RỖNG chứ không phải 0**, kèm dải nhắc đếm số ad như vậy. **Số thật đã kéo về:** 39 campaign · 40 adset · 45 ad · **chi phí 30 ngày = 46,3 triệu** (25/45 ad có chi phí). ⚠️ Chỉ **1/1 tài khoản QC** đang nối vào POS, mà đơn tham chiếu rất nhiều ad khác → **1/64 ad_id trên đơn khớp được** ⇒ muốn ROAS đủ thì phải nối nốt các tài khoản quảng cáo còn lại trong POS. Doanh thu quy nguồn hiện = 0 vì `POS_SYNC_ENABLED` vẫn TẮT (chưa backfill 53k đơn). Kiểm `scripts/thu_quang_cao.py` **47/47 PASS**; hồi quy 10 bộ **350/350 xanh** (tổng 397) |
| 01/08/2026 | **MỤC 4 BRD XONG — Tích hợp Pancake & nguồn quảng cáo** (làm chen trước B8 theo yêu cầu). DB 66→**70 bảng**: `integration_accounts` (kết nối; token THẬT vẫn ở .env, DB chỉ giữ bản che + tình trạng), `sync_logs` (mỗi mẻ đồng bộ 1 dòng), `sync_errors` (hàng đợi retry, giữ NGUYÊN VĂN payload để chạy lại không cần gọi lại Pancake, backoff ×3 tới 5 lần thì `given_up`, unique 1 dòng đang chờ/bản ghi), `staff_mappings` (nhân viên Pancake ↔ CRM); `pages` +account_id/sync_enabled/last_synced_at/last_error; `conversations` +source/external_updated_at/synced_at/assignee_external_id/assignee_user_id/external_tags/snippet/message_count/unread_count (đúng câu "lưu external_id, source, page_id, updated_at_external, synced_at"); customers/orders +synced_at; nới `ads.ad_set_id` NULL để nhận ad_id lẻ Pancake trả (cây campaign/adset là việc C-MVP5) + `lead_attributions` +source/external_ad_id/post_id/utm với unique (customer, touch_type). Code: `integration_repo` · `attribution_repo` · `integration_service` (4 luật mục 4 ghi rõ trong docstring, KHÔNG có hàm ghi ngược Pancake) · `crm_sync` nay đồng bộ thêm **thẻ** (dịch ID→TÊN vì ID thẻ chỉ duy nhất trong 1 page) + **nhân viên xử lý** + tôn trọng công tắc bật/tắt từng page (cache 60s) · `pos_sync` ghi **quy nguồn** (ad_id/post_id/p_utm_*, chạm đầu/cuối tự so mốc nên backfill ngược không dán nhãn sai) + ghi nhận nhân viên POS (KHÔNG tự phân công khách — ownership có luật riêng) · worker mới `sync_retry` (dọn hàng đợi + cảnh báo token 6h/lần, `SYNC_RETRY_ENABLED` mặc định BẬT) · `links.py` dựng link mở hội thoại (mẫu đổi bằng `PANCAKE_CONVERSATION_URL`, chỉ ghép chuỗi — giữ luật "không gọi API mỗi lần mở màn hình"). Web: 4 màn `/quan-tri/tich-hop` (Kết nối · Nhật ký đồng bộ · Danh sách lỗi · Ánh xạ page/nhân viên/trạng thái đơn — màn 23 nay có 2 lối vào), menu mục **Tích hợp** theo quyền `integration.manage`; màn Khách hàng thêm nút 💬 Pancake. API 13 endpoint `/api/v1/integrations/*` + `/customers/{id}/quy-nguon` + `/customers/{id}/pancake-links`. Vá kèm: hội thoại "mồ côi" (thiếu page — hay gặp lúc chạy lại hàng đợi lỗi) trước đây tạo một dòng `pages` external rỗng rồi dồn mọi hội thoại vào đó, nay bị chặn ở `_crm_page_id` và page được kèm vào payload để retry biết đường. Kiểm `scripts/thu_muc4.py` **58/58 PASS**; hồi quy 9 bộ B1→B7 + quản trị đội **292/292 xanh** (tổng 350). Bước kế: **B8 — Bàn giao Sale→CSKH** |
| 01/08/2026 | **Kiểm lại mục 4 trên máy Python 3.11** — `app/web/views/integration.py` dùng backslash trong biểu thức f-string (chỉ 3.12+ cho phép) → app không import được trên máy này; đã sửa 3 chỗ (tách biến/hàm `_o_loi`, HTML giữ nguyên). DB local còn 66 bảng thiếu 6 bảng mới + quyền `ads.view` → nạp lại `init_crm.sql` (66→72) + `seed_auth.py` (18 quyền). Chạy lại nghiệm thu: `thu_muc4` **58/58** · `thu_quang_cao` **47/47**. ⚠️ Từ nay code f-string phải chạy được trên 3.11 (2 máy khác bản Python) |
| 01/08/2026 | **B7 XONG — Đơn hàng + nối Pancake POS** — Dò xong POS Open API (base `pos.pages.fm/api/v1`, api_key riêng theo shop, spec 82 endpoint — `scripts/do_pos_api.py`; shop 1328220779 "Bác sĩ Hội": 53.306 đơn/102 trường, có `conversation_id`+`page_id` nối được về CRM). Schema: `orders.status` 7→**11 trạng thái** (thêm pending/awaiting_shipment/collected/returning), cột `pos_*` + `pos_raw` jsonb + unique (pos_shop_id, pos_order_id), bảng mới `order_status_mappings` seed 17 mã (on conflict do nothing — không đè sửa tay của admin). Luật `order_service`: TRANSITIONS chuyển tay hợp lệ / đồng bộ đi force nhưng LUÔN ghi `order_status_history` (thêm cột reason='pos_sync'), delivered đóng dấu `delivered_at` (đồng bộ lấy mốc THẬT từ status_history POS), đơn đầu/mua lại tự phân loại FR-082 (hủy không tính; so theo mốc TẠO bên POS nên backfill không dán nhãn ngược), giá chốt tại thời điểm bán (Decimal, không tra ngược). Đồng bộ `pancake_pos/pos_sync.py`: khớp khách qua psid/SĐT/hội thoại (KHÔNG dùng customer.id POS — khác không gian id với B2; không sinh lead), idempotent, mã POS lạ nhận 'draft'+reason chờ admin thêm ánh xạ. Worker `pos_orders_loop` poll bù (webhook để khi có domain), công tắc `POS_SYNC_ENABLED` mặc định **TẮT**; quyền mới order.view/order.edit (17 quyền, Sale/CSKH edit — CS01 cần xác nhận đơn, Kế toán view). Kiểm `scripts/thu_b7.py` **50/50 PASS** + hồi quy 8 bộ cũ B1→B6 xanh hết. Chưa làm: backfill 53k đơn (chạy tay khi user chốt), order_items POS (chờ ánh xạ SP), màn 21-23 (khung B11). Bước kế: **B8 — Bàn giao Sale→CSKH** (orders.delivered_at + uq_handovers_order đã sẵn) |
