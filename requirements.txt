# --- Web server ---
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27

# --- Config ---
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# --- Database ---
# Chọn nơi lưu bằng DB_BACKEND trong .env:
#   postgres (mặc định) -> cần `psycopg` + `psycopg_pool` (gói psycopg[binary,pool]).
#                          Vector do pgvector TRONG Postgres lo, phía Python chỉ
#                          gửi literal '[a,b,c]' nên KHÔNG cần gói `pgvector`.
#   supabase            -> cần `supabase` (REST).
psycopg[binary,pool]>=3.1
supabase>=2.4

# --- LLM / Embedding ---
google-generativeai>=0.5
openai>=1.30

# --- Đăng nhập (A2: docs/A2-DANG-NHAP.md) ---
bcrypt>=4.1
pyjwt>=2.8
email-validator>=2.0        # EmailStr trong schemas (A5)
