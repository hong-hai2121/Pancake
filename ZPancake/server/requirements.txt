fastapi>=0.110
uvicorn[standard]>=0.29
python-dotenv>=1.0
httpx>=0.27

# DB: dùng chung Postgres (schema `watcher`) với dự án gốc — db.py mượn pool của
# app/db/client.py nên cần đúng driver này, KHÔNG cần cài thêm gì cho pgvector.
psycopg[binary,pool]>=3.1
# app/config.py (được db.py import gián tiếp) đọc .env bằng pydantic-settings.
pydantic-settings>=2.2
