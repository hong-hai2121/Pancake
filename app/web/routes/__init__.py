"""Router của các trang web.

    main.py      — /, /bang-dieu-khien, /tin-nhan, /khach-hang
    pancake.py   — /pancake/... (xem page, hội thoại, trả lời)
    data.py      — /data/... (kịch bản, hội thoại mẫu, thử bot, thử API)
    sentiment.py — /cam-xuc/... (xem tiêu cực, bật/tắt worker, từ khoá)
"""
