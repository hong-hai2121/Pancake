"""REST API theo đặc tả CRM — chưa có nội dung, xem docs/KE-HOACH-TAI-CAU-TRUC.md.

Base URL `/api/v1`, xác thực `Authorization: Bearer <token>`, mọi phản hồi bọc
trong {success, data, message} hoặc {success, error_code, message, errors}.
"""
